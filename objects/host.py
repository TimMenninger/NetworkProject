# Host Class

class Host:

	def __init__(self, the_hostname):
		''' 
		Initialize an instance of Host by intitializing its attributes.
		'''

		# Name of the host, each hostname is unique string (i.e., "H1")
		self.hostname = the_hostname